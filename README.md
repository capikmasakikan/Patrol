Security Patrol System
- security will scan nfc at designated checkpoint and the data will store in database and show in the admin dashboard.

For admin login:
username: shafiq 
pass: 123

For guard login:
Username: choose any
pin: 1234

For now , the scanning mechanism not working( still figuring out), but I replaced the method with manually enter the NFC UID, everything working fine.

I published the web using netlify : securitypil.netlify.app
But for the backend, still have not publish yet, so it will fully working only locally.
